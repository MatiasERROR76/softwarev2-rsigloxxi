export default (
    _this
) => (
    update,
    callback = (() => {})
) => (
    _this.forceUpdate(
        callback.bind(
            _this,
            Object.assign(
                _this.state,
                update
            )
        )
    )
)
